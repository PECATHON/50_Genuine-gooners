# Python Backend Configuration
NEXT_PUBLIC_PYTHON_BACKEND_URL=http://localhost:8000

# Note: The Python backend must be running for the chat functionality to work
# Start it with: cd backend && python main.py
